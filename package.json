{
  "name": "nvidia-smi-rest-api",
  "version": "1.0.0",
  "description": "This package is a wrapper around the nvidia-smi command to export the results via a REST API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nvidia-smi",
    "rest",
    "api"
  ],
  "author": "Sebastian Schmidt",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3",
    "xml-to-json": "^0.1.1"
  }
}
